name: "manybody-scientific-projection"
version: "0.1.0"
description: "Equation-identical projection backend for the homometric study."

build_config:
  python_version: "3.11"
  package_data:
    - ["../../src/manybody_completion", "/tesseract/manybody_completion"]

metadata:
  backend_contract: "manybody_completion.solvers.SolverBackend"
  reference_backend: "LocalJaxBackend"
