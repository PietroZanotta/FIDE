{
  "experiment": "stage3b-confirmatory-rollout-credit-assignment",
  "status": "predeclared-before-new-seed-execution",
  "scientific_seeds": [406, 407, 408, 409, 410, 411, 412, 413, 414, 415],
  "primary_estimand": "rollout_adapted_minus_frozen_neural interior radial-plus-q4 law MMD2 on untouched evaluation banks",
  "primary_confirmation_rule": "report the paired mean and 95% t interval without retuning; confirmation requires ci95_high < 0",
  "secondary_estimands": [
    "time_dependent_full_gradient_minus_scalar_full_gradient interior law MMD2",
    "time_dependent_full_gradient_minus_stopped_state_gradient interior law MMD2"
  ],
  "frozen_stage3_settings": {
    "modulation": "sigmoid(logit(frozen_gate) + alpha0 + alpha1*cos(2*pi*t) + alpha2*sin(2*pi*t))",
    "learning_rate": 0.04,
    "optimizer": "Adam",
    "optimizer_steps": 40,
    "checkpoint_steps": [0, 5, 10, 15, 20, 25, 30, 35, 40],
    "adaptation_bank_seed_offset": 91000,
    "selection_bank_seed_offset": 92000,
    "evaluation_bank_seed_offset": 93000,
    "generation_count_per_role": 64,
    "oracle_count_per_time_per_role": 256,
    "heun_steps": 24,
    "nfe": 48,
    "evaluation_times": [0.25, 0.5, 0.75, 1.0],
    "adaptation_and_selection_loss": "interior weighted median-RBF MMD2 on measured Phi only",
    "final_metric": "unchanged radial-descriptor-plus-q4 projected-law MMD2",
    "base_model_procedure": "unchanged paper-facing random-continuous-time invariant MLP and Deep-Ritz gate procedure"
  },
  "controls": {
    "scalar_full_gradient": "optimize alpha0 only with alpha1=alpha2=0 through the complete trajectory",
    "stopped_state_gradient": "same three-parameter forward rollout, with the incoming state and Heun proposal stopped at every step during differentiation"
  },
  "prohibited": [
    "hyperparameter tuning",
    "checkpoint changes",
    "architecture or descriptor changes",
    "q4 exposure during adaptation or selection",
    "rerunning alternative designs until one wins"
  ]
}
